# Python
__pycache__/
*.py[cod]
.venv/
.venv-*/
*.egg-info/
.pytest_cache/

# dbt
dbt/target/
dbt/logs/
dbt/packages/
dbt/dbt_packages/

# Data (downloaded, large)
data/raw/

# Env / secrets
.env

# Go
api/bin/
api/*.test

# Derived model artifacts (regenerate with `make train`; serving reads them
# from disk — never commit *products* of training into source control)
artifacts/

# OS / editors
.DS_Store
Thumbs.db
.idea/
.vscode/
# dbt user config (machine-local)
dbt/.user.yml
